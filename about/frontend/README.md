# Frontend (React + Vite)

Install and run:

```powershell
cd frontend
npm install
npm run dev
```

The dev server runs on a port printed by Vite (usually http://localhost:5173). The frontend expects the backend at http://localhost:8000.
